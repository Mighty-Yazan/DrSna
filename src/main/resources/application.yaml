spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/drtooth_db
    username: pguser
    password: pgpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:DrSnaClinicSuperSecretKeyForJwtTokens2026!!}

admin:
  seed:
    full-name: ${ADMIN_SEED_FULL_NAME:System Administrator}
    email: ${ADMIN_SEED_EMAIL:drsna.admin@email.com}
    password: ${ADMIN_SEED_PASSWORD:Admin123!}
    city: ${ADMIN_SEED_CITY:AMMAN}